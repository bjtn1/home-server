services:
  jenkins:
    image: jenkins/jenkins:lts-jdk21
    container_name: jenkins
    restart: unless-stopped
    environment:
      - TZ=America/New_York
    volumes:
      - ./data:/var/jenkins_home
    networks:
      - bjtn-proxy

networks:
  bjtn-proxy:
    external: true
