services:
  arr-control:
    build: .
    container_name: arr-control
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./state:/state
    networks:
      - bjtn-proxy

networks:
  bjtn-proxy:
    external: true
